import React from 'react';
import { ScrollView } from 'react-native';

export const Admin = (user, children) => (
    <ScrollView>
      {hide 
        ? null
        : children
      }
    </ScrollView>
);

export default Filter